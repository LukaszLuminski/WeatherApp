{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Simple weather app consuming API from OpenWeatherMap",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ui": "browser-sync start --proxy=localhost:3000 --files='**/*.css, **/*.js' --ignore=node_modules --reload-delay 10 --no-ui --no-notify"
  },
  "author": "Lukasz Luminski",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.7",
    "express": "^4.17.1",
    "express-sslify": "^1.2.0",
    "hbs": "^4.1.1",
    "path": "^0.12.7",
    "request": "^2.88.2"
  }
}
